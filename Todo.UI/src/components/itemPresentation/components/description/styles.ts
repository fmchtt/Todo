import styled from "styled-components";

export const DescriptionEditContainer = styled.div`
  position: relative;
`;

export const DescriptionEditControls = styled.div`
  position: absolute;

  bottom: -40px;
  right: 0;

  display: flex;
  gap: 10px;
`;
